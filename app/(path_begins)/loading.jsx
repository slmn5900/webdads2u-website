import LoadingComponent from '../../components/LoadingComponent';

const loading = () => {
    return (
        <section>
            <LoadingComponent />
        </section>
    );
};

export default loading;
